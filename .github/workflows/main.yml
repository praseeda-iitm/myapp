name: Test Email Step

on:
  push:
    branches:
      - main  # or your default branch name

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: 23f1000333@ds.study.iitm.ac.in
        run: echo "Hello, world!"
